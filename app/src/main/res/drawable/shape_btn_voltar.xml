<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android">


    <!-- Cor de fundo -->
    <solid android:color="#FF6600" />

    <!-- Bordas arredondadas -->
    <corners android:radius="65dp" />

    <!-- Borda (opcional) -->
    <stroke
        android:width="2dp"
        android:color="#CC5200" />

    <!-- Padding interno -->
    <padding
        android:bottom="8dp"
        android:left="16dp"
        android:right="16dp"
        android:top="8dp" />

</shape>